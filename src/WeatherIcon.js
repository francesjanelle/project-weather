import React from "react"

export default function WeatherIcon(props) {
    const codeMap = {

    }

    return ffffff
     <img
        src={props.info.icon}
        alt={props.info.description} />
}